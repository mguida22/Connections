import React from 'react';
import { AppRegistry } from 'react-native';
import AppRootContainer from './src/AppRootContainer';

const Connections = () => (
  <AppRootContainer />
);

AppRegistry.registerComponent('Connections', () => Connections);
